import React, { FC } from 'react';
import { Text, View } from 'react-native';
import { useSelector } from 'react-redux';
import { Column, Main } from '../store/auth/';

export const MyDesk = () => {
  return (
    <View>
      <Text>test</Text>
    </View>
  );
};
